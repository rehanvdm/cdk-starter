# ~~ Generated by cdk-express-pipeline. To modify, edit your pipeline and run `cdk synth`
name: CDK Express Pipeline Deploy - Prod
concurrency:
  group: cdk-express-pipeline-deploy-prod
  cancel-in-progress: false
on:
  push:
    branches:
      - main
jobs:
  synth__prod:
    name: Synth prod
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Synth
        uses: ./.github/actions/cdk-express-pipeline-synth
        with:
          command: pnpm --filter infra exec cdk synth '**' -c env=prod
            --output=infra/cdk.out/prod
          cloud-assembly-directory: infra/cdk.out/prod
  deploy__prod___shared_infra_starter-global-dev:
    name: "[prod] 📦 shared_infra_starter-global-dev"
    needs:
      - synth__prod
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: shared_infra_starter-global-dev
          cloud-assembly-directory: infra/cdk.out/prod
          command: pnpm --filter infra exec cdk deploy shared_infra_starter-global-dev
            --app=infra/cdk.out/prod --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
  deploy__prod___backend_app_starter-api-dev:
    name: "[prod] 📦 backend_app_starter-api-dev"
    needs:
      - deploy__prod___shared_infra_starter-global-dev
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: backend_app_starter-api-dev
          cloud-assembly-directory: infra/cdk.out/prod
          command: pnpm --filter infra exec cdk deploy backend_app_starter-api-dev
            --app=infra/cdk.out/prod --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
  deploy__prod___backend_app_starter-cron-dev:
    name: "[prod] 📦 backend_app_starter-cron-dev"
    needs:
      - deploy__prod___shared_infra_starter-global-dev
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: backend_app_starter-cron-dev
          cloud-assembly-directory: infra/cdk.out/prod
          command: pnpm --filter infra exec cdk deploy backend_app_starter-cron-dev
            --app=infra/cdk.out/prod --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
  deploy__prod___frontend_app_starter-website-dev:
    name: "[prod] 📦 frontend_app_starter-website-dev"
    needs:
      - deploy__prod___backend_app_starter-api-dev
      - deploy__prod___backend_app_starter-cron-dev
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: frontend_app_starter-website-dev
          cloud-assembly-directory: infra/cdk.out/prod
          command: pnpm --filter infra exec cdk deploy frontend_app_starter-website-dev
            --app=infra/cdk.out/prod --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
