# ~~ Generated by cdk-express-pipeline. To modify, edit your pipeline and run `cdk synth`
name: CDK Express Pipeline Deploy - Stage
concurrency:
  group: cdk-express-pipeline-deploy-stage
  cancel-in-progress: false
on:
  push:
    branches:
      - stage
jobs:
  synth__stage:
    name: Synth stage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Synth
        uses: ./.github/actions/cdk-express-pipeline-synth
        with:
          command: pnpm --filter infra exec cdk synth '**' -c env=stage
            --output=cdk.out/stage
          cloud-assembly-directory: cdk.out/stage
  deploy__stage___shared_infra_starter-global-dev:
    name: "[stage] 📦 shared_infra_starter-global-dev"
    needs:
      - synth__stage
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: shared_infra_starter-global-dev
          cloud-assembly-directory: cdk.out/stage
          command: pnpm --filter infra exec cdk deploy shared_infra_starter-global-dev
            --app=cdk.out/stage --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
  deploy__stage___backend_app_starter-api-dev:
    name: "[stage] 📦 backend_app_starter-api-dev"
    needs:
      - deploy__stage___shared_infra_starter-global-dev
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: backend_app_starter-api-dev
          cloud-assembly-directory: cdk.out/stage
          command: pnpm --filter infra exec cdk deploy backend_app_starter-api-dev
            --app=cdk.out/stage --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
  deploy__stage___backend_app_starter-cron-dev:
    name: "[stage] 📦 backend_app_starter-cron-dev"
    needs:
      - deploy__stage___shared_infra_starter-global-dev
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: backend_app_starter-cron-dev
          cloud-assembly-directory: cdk.out/stage
          command: pnpm --filter infra exec cdk deploy backend_app_starter-cron-dev
            --app=cdk.out/stage --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
  deploy__stage___frontend_app_starter-website-dev:
    name: "[stage] 📦 frontend_app_starter-website-dev"
    needs:
      - deploy__stage___backend_app_starter-api-dev
      - deploy__stage___backend_app_starter-cron-dev
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      id-token: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run CDK Express Pipeline Deploy
        uses: ./.github/actions/cdk-express-pipeline-deploy
        with:
          stack-selector-patterns: frontend_app_starter-website-dev
          cloud-assembly-directory: cdk.out/stage
          command: pnpm --filter infra exec cdk deploy frontend_app_starter-website-dev
            --app=cdk.out/stage --concurrency 10 --require-approval never
            --exclusively
          assume-role-arn: arn:aws:iam::581184285249:role/githuboidc-git-hub-deploy-role
          assume-region: us-east-1
