{
  "name": "@app/backend-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --tsconfig=../../../tsconfig.json --bundle --platform=node --target=node22 --outfile=dist/index.js --sourcemap=linked --minify",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.133",
    "esbuild": "^0.27.2",
    "vitest": "^1.2.2"
  }
}
